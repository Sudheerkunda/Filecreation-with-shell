#/bin/bash
read -p "Please Enter The Filename:" FILENAME
echo "The Filename you have entered is: $FILENAME"
EXISTNAME=$(find / -name $FILENAME | cut -d "/" -f 2)
if [ "$FILENAME" = "$EXISTNAME" ]
then
echo "file already exist"
else
echo "$(mkdir $FILENAME)"
fi